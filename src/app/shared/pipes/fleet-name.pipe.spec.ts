import { FleetNamePipe } from './fleet-name.pipe';

describe('FleetNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FleetNamePipe();
    expect(pipe).toBeTruthy();
  });
});
